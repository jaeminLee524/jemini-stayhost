package com.jemini.stayhost.channel.domain.component;

import com.jemini.stayhost.channel.domain.model.ChannelSyncLog;

public interface ChannelSyncLogManager {

    ChannelSyncLog save(ChannelSyncLog syncLog);
}
